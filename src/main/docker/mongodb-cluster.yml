version: '2'
services:
    ingalom-mongodb:
        image: mongo:4.0.2
        ports:
            - "27017:27017"
        command: mongos --configdb ingalom-mongodb-config
    ingalom-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    ingalom-mongodb-config:
        image: mongo:4.0.2
        container_name: ingalom-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
